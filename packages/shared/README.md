# @lodestone/shared

Shared types, configuration schema, path helpers, network guardrails, and hash
utilities used by Lodestone packages.

This package is an internal workspace dependency for the Lodestone friend
installer package set. It is shipped as a release tarball so the installer can
install all Lodestone packages from pinned GitHub release assets.
