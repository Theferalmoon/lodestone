// SPDX-License-Identifier: Apache-2.0
// `lodestone reindex` stub. Body filled by a later section.
import { output } from "../ui/output.js";

export async function reindex(_argv: readonly string[]): Promise<number> {
  output.warn("`lodestone reindex` is not yet implemented in this build.");
  return 0;
}
