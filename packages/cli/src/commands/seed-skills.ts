// SPDX-License-Identifier: Apache-2.0
// `lodestone seed-skills` stub. Body filled by §11 (seed skill scanners).
import { output } from "../ui/output.js";

export async function seedSkills(_argv: readonly string[]): Promise<number> {
  output.warn("`lodestone seed-skills` is not yet implemented in this build (filled by §11).");
  return 0;
}
