// SPDX-License-Identifier: Apache-2.0
// `lodestone upgrade` stub. Body filled by a later section.
import { output } from "../ui/output.js";

export async function upgrade(_argv: readonly string[]): Promise<number> {
  output.warn("`lodestone upgrade` is not yet implemented in this build.");
  return 0;
}
